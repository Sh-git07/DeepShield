# First Repo
I successfully pushed my first commit to GitHub.
## Dataset
This project uses a real/fake image dataset for training.
Place your dataset inside a `data/` folder in this structure:
```
data/
  train/fake/
  train/real/
  val/fake/
  val/real/
  test/fake/
  test/real/
```
